# CLAUDE.md

At the start of every session in this repo:

1. Read each file linked under **Project** in the auto-loaded memory index
   in full — the index only gives one-line summaries, the linked files have
   the real detail. Then skim the **Feedback & conventions** entries.
2. Read `AGENTS.md` at the repo root in full. It is the authoritative
   technical briefing: working rules, architecture, conventions, pitfalls,
   and the "done" checklist.

@.claude/HANDOFF.md
