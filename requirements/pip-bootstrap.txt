# Pins the pip version used to bootstrap the Docker builder stage venv
# (docker/Dockerfile) before it installs runtime.txt. Kept as its own tiny
# requirements file (rather than an unpinned version floor inline in the
# Dockerfile) so:
#   - it satisfies OpenSSF Scorecard's Pinned-Dependencies check (hash-pinned,
#     like every other dependency in this repo)
#   - Renovate can see and bump it automatically (it matches the same
#     "requirements/*.txt" pattern as runtime.txt/ci.txt), picking up future
#     pip releases — including security fixes — under the same 14-day
#     minimumReleaseAge + automerge policy as everything else
pip==26.2.1 \
    --hash=sha256:71138adf1f4ca900cdb7d289c21b7494329f2332b6d85f0e1c42108c0384ed3e \
    --hash=sha256:f6ad667e89a1fe78046c8f13232b247200f5258d7828f3f7883d660878e0813f
