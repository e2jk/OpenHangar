# Development, test, and tooling dependencies.
# Install into your local venv:
#   pip install -r requirements/dev.txt
#   playwright install chromium   # only needed for scripts/take_screenshots.py
#
# Versions are pinned to match CI (requirements/ci.txt) exactly so that
# linting, formatting, and test results are identical locally and in the pipeline.
pytest==9.1.1
pytest-cov==7.1.0
pytest-xdist==3.8.0
bcrypt==5.0.0
cryptography==49.0.0
qrcode[pil]==8.2
pyotp==2.10.0
flask-babel==4.0.0
polib==1.2.0
ruff==0.15.20
mypy==2.1.0
openpyxl==3.1.5
# Screenshot script (scripts/take_screenshots.py) — run `playwright install chromium` after
playwright==1.60.0
PyYAML==6.0.3
