# Development, test, and tooling dependencies.
# Every package here is already pinned (with hashes) in requirements/ci.txt,
# which itself layers on requirements/runtime.txt — so this just installs
# from there instead of duplicating versions that could drift out of sync
# with what CI actually runs (this file used to pin qrcode[pil]/pillow by
# hand for exactly that reason — GitHub's dependency graph had flagged an
# alert here after the unconstrained [pil] extra resolved pillow
# independently of runtime.txt's own pin; runtime.txt pinning pillow
# directly, now inherited here too, already prevents that).
#
# Install into your local venv:
#   pip install -r requirements/dev.txt
#   playwright install chromium   # only needed for scripts/take_screenshots.py
#
# actionlint is NOT here — it's a standalone Go binary, not a pip package.
# .githooks/pre-push prints an install one-liner if it's missing from PATH.
-r ci.txt
