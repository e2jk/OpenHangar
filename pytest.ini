[pytest]
testpaths = tests
pythonpath = app
addopts = -n auto
markers =
    e2e: end-to-end browser tests via Playwright (run with --e2e)
    destructive: modifies persistent server data (skipped against live dev server)
