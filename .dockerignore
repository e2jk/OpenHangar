# Exclude everything from the build context by default,
# then explicitly allow only what the Dockerfile COPYs.
**

!docker/docker-requirements.txt
!docker/docker-entrypoint.sh
!docker/wait-for-postgres.py
!app/
!demo/
