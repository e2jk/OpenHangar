version: 2
updates:
  # Keep GitHub Actions SHA-pinned versions current
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday

  # All pip requirements (runtime.txt, ci.txt, dev.txt)
  - package-ecosystem: pip
    directory: /requirements
    schedule:
      interval: weekly
      day: monday
